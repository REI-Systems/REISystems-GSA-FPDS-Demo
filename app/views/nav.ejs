
<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top" ng-controller="CommonController">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#/">FPDS Demo</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li ng-show="isUserAuth">
                    <a href="#/search">Search</a>
                </li>
                <li ng-show="isUserAuth">
                    <a href="#/api">API</a>
                </li>
                <li ng-show="isUserAuth">
                    <a href="#/feedback">Feedback</a>
                </li>
                <li>
                    <a href="#/about">About</a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li ng-show="isUserAuth">Welcome, {{ user.name }} !</li>
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="javascript:void(0);">
                        <i class="fa fa-user fa-fw"></i>  <i class="fa fa-caret-down"></i>
                    </a>
                    <ul class="dropdown-menu dropdown-user">
                        <!-- Is user logged out -->
                        <li ng-show="!isUserAuth">
                            <a href="#/login"><i class="fa fa-sign-in fa-fw"></i> Login</a>
                        </li>
                        <li ng-show="!isUserAuth">
                            <a href="#/register"><i class="fa fa-user fa-fw"></i> Register</a>
                        </li>
                        <!--Is user logged in -->
                        <li ng-show="isUserAuth">
                            <a ng-href="#/user/edit/{{ user.id }}"><i class="fa fa-pencil fa-fw"></i> Profile</a>
                        </li>
                        <li ng-show="isUserAuth" class="divider"></li>
                        <li ng-show="isUserAuth">
                            <a href="javascript:void(0);" ng-click="logOut();"><i class="fa fa-sign-out fa-fw"></i> Logout</a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>